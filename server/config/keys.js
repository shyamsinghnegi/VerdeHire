// This file can be used for configuration variables,
// but for now, we'll mostly rely on environment variables directly.
// You might put things like API keys here if you don't use .env or
// want to structure them differently.

module.exports = {
  // jwtSecret: process.env.JWT_SECRET // Can be accessed directly from process.env if loaded with dotenv
};